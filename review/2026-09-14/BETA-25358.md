# ENCA beta 25358 — rollout sources and branded sidebar

Changes on `beta`, following `4cb45ab`. Guided rollout T40 is now version 1.1.

## Delivered

- Scope and Plan offer **CloudFellows — Choose ZIP** and **Joey Verlinden — Fetch latest**.
- CloudFellows opens the existing backup ZIP picker and import review. Reopening clears the file input so the same file can be selected again.
- Joey forces a fresh repository read and hands the complete bundle, including groups and named locations, to the existing import review. Release and commit identify the source. Failed refreshes, empty policies, incomplete bundles and skipped dependency files stop preparation, even when an older cached bundle exists. Repeated clicks cannot duplicate preparation; a tenant switch discards a pending fetch result.
- Shared E-Admins policies are identified as coming from the CloudFellows ZIP. Loading a source does not change the selected tenant policies or authorize a write; existing import confirmations remain in place.
- The desktop sidebar uses the active brand's deep colour and accent, with neutral text and derived hover/muted colours. Active tool numbers inherit the selected row's text colour. The mobile sidebar retains the active theme's surface/ink tokens.
- Build/asset stamps, changelog and Waiting for production item 194 were updated together. The previous report's first push command now explicitly targets `origin/beta`.

## Completed validation

- **34 offline regression tests passed**, including new rollout source dispatch, successful complete-bundle preparation, dependencies, duplicate-click suppression, stale-cache rejection/retry, incomplete and failed fetches, and tenant-switch cancellation.
- All application JavaScript parses and local script assets exist. Release/asset/promotion metadata checks pass.
- Toolbar structure: 34 tool screens, 33 toolbars, all in slot order, with the existing Protect exception.
- Changelog, promotion queue and tool notes pass the plain-text check. Git whitespace check passes.
- Isolated Edge browser, demo mode: Scope and Plan source buttons; native ZIP picker and CloudFellows prompt; fresh Joey adapter into the actual import review with release/commit; failed refresh with an older bundle does not reopen import. No page errors. At 390px there is no horizontal document overflow.
- Browser branding fixture: purple sidebar background in light, dark and system-dark modes; hover and selected-row colours; mobile navigation uses the fixture's dark surface at 390px. Desktop screenshots inspected.

## Still required before production

The Joey browser read used a controlled fixture, not a live GitHub release. Validate a real release and its dependencies, a real CloudFellows backup ZIP, and target-tenant assignment/permission behaviour before import. No real tenant writes or Docker deployment checks were performed in this update. Earlier outstanding validation in BETA-25357.md remains outstanding.

## Push

```powershell
git push origin beta:beta
git push enca-beta beta:main
```

These target the beta branch in `nurejev/enca` and the site branch in `nurejev/enca-beta`, respectively.
