# Beta 25361 — remove obsolete September rollout controls

Date: 2026-09-14. Tool: SMS & voice retirement (T33 1.6).

Removed the Microsoft September rollout / passkey dynamic migration panel, including re-check, pause/resume, acknowledgement and change-history controls. Removed the supporting state, authentication-method policy write path, audit parsing and unused styles. Updated the home tile, Help, tool note and Markdown report to match. General retirement information remains.

The tenant scan, registered-method column and default markers, phone roles, verdict filters, notification draft and Markdown/CSV exports remain available. Existing scan permissions remain; the tool no longer offers a tenant-setting write. The sidebar theme proposal remains a separate mockup and is not implemented by this change.

## Verified

- 37 offline regression tests passed, including scan verdicts, method data in exports, affected notification recipients, and scope-only fallback when registration data is unread.
- Toolbar ordering and plain-text checks passed.
- Edge demo: initial screen has no migration panel; scan returns four expected users; all four populated verdict filters work; rescan restores all rows; registered-method labels remain visible.
- Markdown contains registered methods and no opt-out advice. CSV downloads with both method columns and all four users.
- Notification draft contains Maria and Pieter; no message was sent.
- No browser JavaScript errors. Desktop (1600 px) and mobile (390 px) screenshots inspected: the removed panel leaves no empty container; mobile retains the existing horizontally scrollable results table.
- JavaScript syntax, release metadata/cache version consistency and git whitespace checks passed.

## Pending before production

- Real tenant with AuditLog.Read.All: compare users, verdicts and method lists with build 25360. Confirm no pause/resume actions or authentication-method write consent are offered.
- Real tenant declining registration consent: confirm scope-only output and unknown registration data.
- Live tenant and deployment tests were not run. This remains in Waiting for production, queue item 197.

## Push after review

```powershell
git push origin beta:beta
git push enca-beta beta:main
```

These commands publish beta; they have not been run by the assistant.
