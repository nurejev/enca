# Beta 25363 — approved cream sidebar

Date: 2026-09-14. Shared navigation appearance.

Implements the displayed cream-sidebar mockup in Dark mode: exact #FCF8E8 surface, branded deep text and selection, cream selected text and focus outline. Auto dark mode, collapsed desktop navigation and the mobile drawer share the palette. Light mode retains the dark branded sidebar requested earlier. The optional full-page and introduction-background concepts are not included.

Cream has a dedicated workspace token. It does not reuse or modify the warning palette, so status cards keep their meaning. App build and asset URLs are 25363; individual tool versions remain unchanged for this shared CSS change.

## Verified

- Edge demo, default green and the approved purple mockup palette: exact background RGB 252/248/232 in Dark mode.
- Light, Dark, Auto OS changes and explicit-mode overrides all pass.
- Normal, muted and selected text contrast exceeds 4.5:1; lowest measured ratio 6.6756:1.
- Hover, keyboard focus, collapsed 56 px rail, 270 px mobile drawer at 390 px viewport, mobile navigation and no page overflow checked.
- No browser JavaScript errors; screenshots inspected.
- 37 offline regression tests pass. Toolbar ordering, plain-text release notes, build/asset/queue consistency and whitespace checks pass.

## Waiting for production

Queue item 199 records the change and validation. Pending: refresh hosted beta and check the deployed customer brand configurations. No live-tenant or deployment test was run.

```powershell
git push origin beta:beta
git push enca-beta beta:main
```

Push commands have not been run.
